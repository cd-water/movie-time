<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cdwater.movietimeuser.mapper.UserMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(phone, nickname, avatar)
        VALUES (#{phone}, #{nickname}, #{avatar})
    </insert>
    <update id="updatePwdByPhone">
        UPDATE user
        SET password =#{newPassword}
        WHERE phone = #{phone}
    </update>
    <update id="updateById">
        UPDATE user
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthday != null">birthday = #{birthday},</if>
            <if test="introduce != null">introduce = #{introduce},</if>
        </set>
        WHERE id = #{id}
    </update>
    <select id="selectPage" resultType="com.cdwater.movietimeuser.model.vo.UserPageVO">
        SELECT
        phone,
        nickname,
        avatar,
        gender,
        birthday,
        introduce,
        create_time,
        update_time
        FROM user
        <where>
            <if test="phone != null and phone != ''">
                AND phone LIKE concat('%', #{phone}, '%')
            </if>
            <if test="nickname != null and nickname != ''">
                AND nickname LIKE concat('%', #{nickname}, '%')
            </if>
        </where>
        ORDER BY create_time
    </select>
    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM user
    </select>
    <select id="selectByPhone" resultType="com.cdwater.movietimeuser.model.entity.User">
        SELECT id,
               phone,
               password,
               nickname,
               avatar,
               gender,
               birthday,
               introduce,
               create_time,
               update_time
        FROM user
        WHERE phone = #{phone}
    </select>
    <select id="selectById" resultType="com.cdwater.movietimeuser.model.entity.User">
        SELECT id,
               phone,
               password,
               nickname,
               avatar,
               gender,
               birthday,
               introduce,
               create_time,
               update_time
        FROM user
        WHERE id = #{id}
    </select>
    <select id="selectByIds" resultType="com.cdwater.movietimeuser.model.vo.UserListVO">
        SELECT id, nickname, avatar
        FROM user
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    <select id="selectFollow" resultType="com.cdwater.movietimeuser.model.entity.UserFollow">
        SELECT follower_id, followee_id
        FROM user_follow
    </select>
    <insert id="insertFollow">
        INSERT IGNORE INTO user_follow (follower_id, followee_id)
        VALUES (#{followerId}, #{followeeId})
    </insert>
    <delete id="deleteFollow">
        DELETE
        FROM user_follow
        WHERE follower_id = #{followerId}
          AND followee_id = #{followeeId}
    </delete>
</mapper>